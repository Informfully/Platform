import {
    COLORS,
    FONT_WEIGHT,
    FONT_SIZE,
    FONT_FAMILY,
    FONT_STYLE,
    DARK_COLORS,
} from './variables';

import { GLOBAL_STYLES, globalStyleGenerator } from './globals';
import { PUBLIC_STYLES, publicStyleGenerator } from './public';
import { NEWS_ARTICLES_STYLES, newsArticlesStyleGenerator } from './newsArticles';
import { DynamicColors, withColor } from './DynamicColors';

export {
    COLORS,
    DARK_COLORS,
    FONT_WEIGHT,
    FONT_FAMILY,
    FONT_SIZE,
    FONT_STYLE,
    GLOBAL_STYLES,
    globalStyleGenerator,
    PUBLIC_STYLES,
    publicStyleGenerator,
    NEWS_ARTICLES_STYLES,
    newsArticlesStyleGenerator,
    DynamicColors,
    withColor,
};
